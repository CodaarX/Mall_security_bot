var name 
var age 
var maskChecker = prompt("do you have a facemask?")

var totalVisitors = 0

var deniedVisitors = 0

var allowedVisitors = 0


function startChecker(){
    name = prompt("Input your name")
    if (name === null) {
        startChecker
    }   age = prompt("input your age")
            if(age === null){
            age = prompt ("inut your age")
    }           
}